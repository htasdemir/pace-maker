project:
  name: "Simple Pacemaker Controller"
  owner: "Your Name"
  github_user: "htasdemir"
  description: |
    A simple digital pacemaker controller that generates a pacing pulse
    if no heartbeat is detected within a preset timeout interval.

  language: "Verilog"
  clock_hz: 12000000
  inputs: 2
  outputs: 1
  bidirectional: 0
  sequential: true
  testbench: "tb.v"
  source_files:
    - "src/project.v"
    - "src/user_module.v"

# Optional logo file (240x240 PNG)
# logo: "logo.png"
