project:
# Tiny Tapeout project information
  name: "Simple Pacemaker Controller"
  owner: "Your Name"
  github_user: "htasdemir"
  description: |
    A simple digital pacemaker controller that generates a pacing pulse
    If no heartbeat is detected within a preset timeout interval.

  language: "Verilog"
  clock_hz: 12000000  # Tiny Tapeout standard clock (12 MHz)
  inputs: 2           # Heartbeat sensor input, Reset input
  outputs: 1          # Pacing pulse output
  bidirectional: 0
  sequential: true
  testbench: "tb.v"
  source_files:
    . "project.v"
    . "and.v"
# If you have a logo (PNG, 240x240), you can add it like this:
     logo: "logo.png"  
